Разработать программу на языке ассемблера, которая запрашивает у пользователя ввод произвольного текста (строки), преобразует введенную строку в соответствии с вариантом и выводит полученую строку на экран.

Считать, что пользователь всегда вводит строку достаточной длины.
Вычисления выполняются над кодами символов.
Перед вводом строки вывести поясняющий текст.
После вывода результата дождаться нажатия клавиши.

При изменении строки использовать обращение к памяти различными способами.

S3 <-> S9, S2 <- S4 - (S5 - S6)

Si означает i-й символ строки. Действия обозначены следующим образом: a <-> b - поменять местами a и b; a <- b - в а записать значение b

Develop an assembly language program that requests the user to enter an arbitrary text (string), converts the entered string according to the variant and displays the resulting string on the screen.

Assume that the user always enters a string of sufficient length.
Calculations are performed on character codes.
Before entering the string, display the explanatory text.
After the result is displayed, wait for the key to be pressed.

When changing a string, use memory access in various ways.

S3 <-> S9, S2 <- S4 - (S5 - S6)

Si stands for the i-th character of the string. Actions are labeled as follows: a <-> b - swap a and b; a <- b - write the value of b into a